# Where the frontend looks for the API.
#
# Local dev: leave this unset — vite.config.js proxies /api to localhost:5000.
# Production: set it to your deployed backend URL, e.g.
#   VITE_API_URL=https://webtrack-api.onrender.com/api
VITE_API_URL=
